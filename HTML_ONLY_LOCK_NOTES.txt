沅興木業 HTML 全頁面鎖定版

本版原則：
1. 庫存 / 訂單 / 總單 / 出貨 / 倉庫頁面外殼、工具列、按鈕、卡片容器由 templates/module.html 固定。
2. JS 只更新資料列、商品下拉、格位內容、預覽結果，不再重建整頁外殼。
3. 停用庫存/訂單/總單與客戶區 MutationObserver 舊版修復掃描。
4. 非首頁不再載入 fix135~fix142 舊 FIX CSS，避免覆蓋新介面。
5. html_direct_master_lock.js 只負責封鎖舊 renderer，不再重複安裝模組。
6. 倉庫清除搜尋改為資料高亮清除，不再呼叫 renderWarehouse(true)。
7. 已移除 ship_single_lock.js.bak，避免後續誤載。

之後除非修改 templates/module.html 或你明確說要改 HTML，否則不要改頁面按鈕與卡片外觀。
