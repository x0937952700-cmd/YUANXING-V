FIX28 正式收斂升級版

已完成：
1. 清掉舊 fix18～fix27 前端補丁載入，只保留 app.js + fix28.js。
2. 倉庫圖拖拉加入確認、客戶/數量帶入、防超量仍由後端檢查。
3. 尚未入倉商品加入來源篩選：全部 / 庫存 / 訂單 / 總單，支援選數量拖拉與手動格位。
4. 庫存 / 訂單 / 總單卡片可編輯、刪除、轉庫存、移訂單、移總單、直接出貨。
5. 新增 /api/items/transfer 與訂單、總單單筆編輯 API，讓四大資料流互通。
6. 今日異動加上操作紀錄中心，可搜尋、篩選、點開查看 before/after。
7. UI 增加 App 化底部彈窗、卡片陰影、手機按鈕與拖拉反饋。
8. 保留原有 app.js、OCR、客戶、設定、備份、倉庫、代辦等功能，不刪功能，只整理和強化。
