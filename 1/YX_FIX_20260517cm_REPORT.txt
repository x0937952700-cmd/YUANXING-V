YX_FIX_20260517cm_REPORT
基準：20260517j 滿意版 / 20260517cl 出貨扣倉庫圖預檢較大包
範圍：只加出貨扣倉庫圖「扣後讀回確認」，不改訂單、總單、庫存扣除邏輯，不改倉庫資料結構。

修改檔案：
- app.py
- db.py
- static/yx_modules/ship_single_lock.js
- static/yx_modules/yx_20260517_user_request.css

修復內容：
1. 出貨同步扣 warehouse_cells.items_json 後，回傳 readback_cells。
2. 讀回被扣過的格位是否仍存在、剩餘品項筆數與件數。
3. /api/ship-warehouse-readback-check 新增只讀檢查端點，可用出貨回傳 warehouse_sync 做扣後讀回。
4. 出貨完成畫面新增「扣後讀回確認」，手機可橫向滑動查看。
5. 出貨主線仍是先扣訂單/總單/庫存，倉庫圖不足只警告不 rollback。

風險控制：
- 不改 DB schema。
- 不新增 renderer。
- 不新增 setInterval / MutationObserver。
- 不寫額外倉庫資料，只在原同步扣除後做讀回確認。
