沅興木業 FULL MASTER V24 SAFE PERSIST NO-EVENT-CHANGE AUDIT

本版原則：不改動既有頁面、按鈕、事件、介面流程與主要邏輯，只修復已知問題與後端永久保存 / migration / 快取版本。

已修復：
1. 庫存 / 訂單 / 總單送出後若遇到重複 request_key，後端會回傳目前 DB 真實清單與 snapshots，不再讓前端保留 tmp 暫存列，避免刷新後消失。
2. 送出失敗時會清除暫時顯示的 optimistic rows，避免畫面顯示未寫入資料。
3. 今日異動每次開啟固定先顯示「新增訂單」單一卡片新版，不再讀取上次 all/進貨/出貨篩選造成先跳舊三區塊再跳新版。
4. 倉庫批量下拉改用穩定 item key；按「新增更多批量」、搜尋、重新載入未錄入清單時，不會因 index 改變而清空或選錯商品。
5. 倉庫「儲存格位」仍走 /api/warehouse/cell 永久保存，保留長按開啟 / 插入 / 刪除、拖拉換位。
6. db.py 增加 V24 safe persistence overrides：submit request 以 endpoint 分流，避免不同 API request_key 互擋；庫存 / 訂單 / 總單寫入加上 rollback/finally close，降低 PostgreSQL pool leak 風險。
7. 靜態檔版本升級為 full-master-v24-safe-persist-no-event-change，強制 Render / 手機 / PWA 重新抓最新 HTML / JS / CSS，不吃舊快取。

驗證：
- python -m py_compile app.py db.py wsgi.py tools/smoke_test.py：通過
- node --check static/yx_pages/*.js static/pwa.js static/service-worker.js：通過
- python tools/smoke_test.py：通過
