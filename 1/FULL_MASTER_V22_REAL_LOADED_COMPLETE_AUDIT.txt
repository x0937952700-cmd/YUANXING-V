FULL MASTER V22 REAL LOADED COMPLETE AUDIT

實際修正：
1. 實際載入版本改為 full-master-v22-real-loaded-complete。
2. base.html 只載入 page_*_master_v22.js，不再載入 page_*_master_v21 / v20 / v19 / v2。
3. app.py 增加 yx_v22_product_snapshots()，批量材質 / 批量 A/B 區 / 批量刪除 / 批量編輯 / 批量轉入全部回傳最新清單 snapshots 與 customers。
4. 庫存 / 訂單 / 總單頁實際載入 JS 增加 applySnapshotFromResponse()：API 成功後直接用後端最新清單重畫，不再靠下一次手動刷新。
5. 訂單 / 總單確認送出後優先使用後端回傳的 exact_customer_items / snapshots / customers，並背景 ensure 客戶，不阻塞畫面。
6. pwa.js 修正非法 window.__YX_full-master... 變數，避免 PWA JS 解析失敗。
7. style.css 加入 V22 實際載入樣式守門，保留 toast、小卡、出貨一排、客戶卡、批量編輯列的顯示規則。
8. 刪除舊 audit / README 類文字檔，避免 GitHub 包內混亂。

檢查結果：
- 新增外部補丁 script：0
- 實際載入 page_*_master_v2.js：0
- 實際載入 v9～v21 快取字串：0
- HTML / JS / CSS / app.py 已同時修改
- Python compile OK
- JS syntax OK
- tools/smoke_test.py OK

保留的 setTimeout：只用於 toast 自動消失、長按判定、倉庫高亮動畫，不是舊版重畫 renderer。
保留的多個 click listener：屬於同頁母版內分區功能（商品表格、客戶區、封存客戶），不是外部補丁載入。
