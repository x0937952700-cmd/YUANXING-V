
不要 overlay，不要 hardlock，不要 setInterval / MutationObserver 硬塞按鈕。
所有修改都要直接寫進主檔：
HTML / JS / CSS / app.py / db.py / requirements.txt / wsgi.py / render.yaml / Procfile / PostgreSQL migration。
倉庫資料不能清空，不能重排有商品格，不能洗掉 warehouse_cells。
倉庫每欄預設顯示 20 格，DB 缺格時只補缺少空格。
新增 / 插入 / 刪除格子要連動 DB。
庫存 / 訂單 / 總單批量編輯儲存後要立刻退出編輯狀態並顯示修改後資料，再背景寫 DB。
不要清空 warehouse_cells。
不要重建倉庫資料表後不搬資料。
不要把每欄洗成只剩 1 格。
不要把 20 格預設顯示改成只顯示 DB 現有格。
不要用補丁檔硬塞功能。
不要同時保留多個 renderer。
不要同一個按鈕綁多個 click。
不要 setInterval 補按鈕。
不要 MutationObserver 補按鈕。
不要只改 JS，不改 HTML / CSS / app.py / db.py。
不要改掉 V60 原本正常功能。









一、庫存需要改什麼
1. 庫存清單按鈕補齊

庫存清單固定要有：

搜尋商品 / 客戶 / 材質 / A區
全部區
A區
B區
批量增加材質
套用材質
批量刪除
批量編輯全部
儲存批量編輯
加到訂單
加到總單
移到A區
移到B區
2. 批量編輯邏輯

按「批量編輯全部」時：

如果有勾選商品，只編輯勾選商品
如果沒有勾選商品，直接編輯全部商品
材質欄位要是下拉選單
可改材質、月份、尺寸、支數 x 件數、A/B 區

按「儲存批量編輯」後：

前端立刻套用修改後資料
立刻退出編輯狀態
立刻顯示修改後清單
後台再寫入 DB
寫入失敗才提示並回讀資料庫還原
3. 批量材質
選擇材質後按「套用材質」
有勾選就套用勾選商品
沒勾選就套用目前篩選清單
套用後立刻刷新畫面
寫入 inventory DB
4. 庫存新增 / 送出
商品資料送出不能出現 pushProductUndo is not defined
送出後要真的寫入 inventory
送出成功後立刻出現在庫存清單
不要只顯示成功但 DB 沒寫入
失敗要顯示明確錯誤
5. 庫存加到訂單 / 總單
庫存商品可以加到訂單
庫存商品可以加到總單
加入後要寫入 orders / master_orders
不可只改前端
原本庫存資料是否保留，照 V60 原邏輯，不要亂改
6. 庫存移到 A/B 區
商品可批量移到 A區
商品可批量移到 B區
area / location 要寫進 DB
清單立即更新
二、訂單需要改什麼
1. 訂單清單按鈕補齊

訂單清單表頭固定要有：

加到總單
移到A區
移到B區
批量刪除
批量編輯全部
儲存批量編輯

訂單上方工具列保留：

搜尋
全部區
A區
B區
批量增加材質
套用材質

不要用 setInterval / MutationObserver 硬塞按鈕。
要直接寫進訂單頁主 renderer。

2. 訂單小卡功能

每筆訂單商品小卡要有：

編輯
刪除
直接出貨
加到總單

點北 / 中 / 南客戶後：

下方立刻顯示該客戶商品
不要延遲 3 秒
不要等後端慢慢重刷才出現
3. 訂單批量編輯

同庫存邏輯：

有勾選只編輯勾選
沒勾選編輯目前清單全部
儲存後立刻退出編輯狀態
前端立刻顯示修改後狀態
後台寫入 orders DB
4. 訂單批量刪除
有勾選刪除勾選
沒勾選不可誤刪全部，需提示
刪除後前端立即移除
後台刪 orders DB
失敗要還原
5. 訂單加到總單
勾選訂單商品後可加到總單
要寫入 master_orders
不可只新增前端
加入後總單頁要看得到
同尺寸可依原本邏輯合併或新增，不要破壞 V60 原邏輯
6. 訂單移到 A/B 區
勾選商品後可移到 A區 / B區
寫入 orders.area / orders.location
畫面立即更新
7. 訂單客戶區

北 / 中 / 南客戶標籤要保留：

長按開操作表
右鍵開操作表
編輯客戶
移到北 / 中 / 南
封存
刪除
pointer 拖拉換區
拖拉時不可跳長按選單
三、總單需要改什麼
1. 總單清單按鈕補齊

總單清單表頭固定要有：

移到A區
移到B區
批量刪除
批量編輯全部
儲存批量編輯

總單上方工具列保留：

搜尋
全部區
A區
B區
批量增加材質
套用材質

不要用補丁硬塞。
直接寫進總單頁主 renderer。

2. 總單小卡功能

每筆總單商品小卡要有：

編輯
刪除
直接出貨

總單畫面不要顯示不該出現的客戶欄位，照原 V60 規則保留。

3. 總單批量編輯
有勾選只編輯勾選
沒勾選編輯目前清單全部
儲存後立刻退出編輯狀態
立刻顯示修改後資料
後台寫入 master_orders DB
4. 總單批量刪除
有勾選刪除勾選
沒勾選不可誤刪全部，需提示
後台刪 master_orders
前端立即更新
5. 總單移到 A/B 區
商品可批量移到 A區 / B區
寫入 master_orders.area / location
前端立即更新
6. 總單客戶區

北 / 中 / 南客戶標籤同訂單：

長按操作表
右鍵操作表
編輯
移區
封存
刪除
pointer 拖拉換區
四、倉庫圖需要改什麼
1. 倉庫基本結構

倉庫圖固定為：

A區 / B區
各 6 欄
每欄預設 20 格
格數可手動新增
格數可手動刪除
DB 缺格時只補缺少的空格

絕對不能：

清空 warehouse_cells
delete all 後重建
把每欄洗成 1 格
重排有商品格
清掉格內商品資料
2. 格子顯示格式

每格只顯示簡潔資訊：


   永和 DF  132X11X013  6(紅字)
   保固 SP  131X10X013 2(紅字)
   6+2              8件

規則：

第一行左側：格號，只顯示數字

第二行 客戶名，紅字 材質 商品尺寸 如果有不同尺存則再多出一行 該尺寸總件數(紅字)
  
第三行左側：件數拆分，例如 4+2+1
第三行右側：總件數，例如 7件，藍字
空格顯示「空格」
未指定客戶顯示「庫存」
不顯示 FOB / CNF / FOB代付
不顯示尺寸全文
3. 點格子編輯

點格子後開啟格位編輯彈窗：

上方：

顯示目前格位
顯示此格目前商品

中間：

搜尋已錄入商品
批量加入商品

批量加入預設三列：

後排
中間
前排

每列：

下拉選未入倉商品
加入按鈕

功能：

可新增更多批量列
可刪除格內商品
可編輯備註
儲存後寫入 DB
儲存後前端立即更新
4. 未入倉商品下拉

下拉選單來源：

庫存
訂單
總單

規則：

A區只顯示 A區未入倉商品
B區只顯示 B區未入倉商品
已加入倉庫的商品要從下拉移除
從格子刪除商品後，要回到未入倉下拉
下拉要顯示客戶、材質、尺寸、支數 x 件數
5. 新增 / 插入 / 刪除格子

長按或右鍵格子開啟操作表：

開啟 / 編輯格位
在此格後插入格子
刪除此格
返回該格

新增 / 插入：

前端立刻顯示
後台寫入 DB
後面格號往後移
不可覆蓋有商品格

刪除：

只允許刪空格
有商品格不可刪
刪除後後面格號往前補
DB 同步更新
前端立即更新
失敗要還原
6. 解決「資料庫沒有格子」

正確做法：

前端仍可預設顯示 20 格
後端收到操作時，如果 DB 缺該格，只補那一格或缺少範圍
只補空格
不清空、不重排有商品資料
slot_type 空字串要當 direct

SQL 要用：

COALESCE(NULLIF(slot_type,''),'direct')
7. 拖拉移動
支援滑鼠拖拉
支援手機 pointer drag
不用舊 HTML5 drag
拖拉開始後取消長按
拖拉後前端立即更新
後台寫入 DB
失敗才還原
拖拉後件數重新計算
8. 搜尋
可搜尋商品 / 客戶 / 格位
搜尋不能卡住
搜尋結果高亮
清除搜尋即時恢復
不要整頁重 render
不要搜尋時一直重算未入倉
9. 統計

顯示：

A區 x 件
B區 x 件
總計 x 件

統計來源要從商品資料結構算，不靠畫面文字猜。

五、出貨需要改什麼
1. 出貨頁不能卡住
點出貨頁要立即打開
商品資料不能等好幾秒
下拉選單不能亂跳
不要舊 renderer 重複載入
2. 出貨商品來源

選客戶後，下拉選單要顯示該客戶所有商品：

訂單商品
總單商品
必要時庫存商品
3. 出貨扣除邏輯

出貨時：

如果商品來自總單，扣 master_orders
如果商品來自訂單，扣 orders
如果沒有對應訂單 / 總單，才扣 inventory
扣除前要檢查數量
不足要提示
4. 出貨預覽

確認前要顯示：

商品
客戶
材質
尺寸
支數 x 件數
扣前數量
扣後數量
材積 / 重量計算結果
5. 出貨成功
寫入 shipping_records
寫入 today_changes
更新來源表數量
前端立即刷新
不可只顯示成功但 DB 沒扣
六、今日異動需要改什麼
1. 固定新版格式
只保留新版直列卡片
不保留舊橫排格式
不可先跳舊版再跳新版
2. 手動刷新
保留刷新按鈕
點刷新才重新抓資料
不要自動一直刷新
不要 MutationObserver 監控整頁
3. 顯示內容

今日異動顯示：

誰操作
什麼時間
做了什麼
哪個客戶
哪個商品
哪個位置
4. 未入倉
未入倉統計手動刷新
不要每次進頁面都重算
不要背景一直算造成卡頓
七、客戶標籤 / 北中南需要改什麼

訂單、總單、出貨相關客戶標籤要保留：

北區
中區
南區

每個客戶標籤：

字體放大
點擊後立刻顯示商品
長按開操作表
右鍵開操作表
pointer 拖拉換區
可編輯客戶
可移到北 / 中 / 南
可封存
可刪除

拖拉要求：

手指或滑鼠移動後取消長按
不可拖拉時又跳出編輯表
不用 HTML5 drag
用 pointer 事件

2. 白板格式解析

支援：

公司名
132x23x05=249x3

也支援：

×
✕
*
全部正規化成 x
3. 件數規則

必須符合：

60+54+50 = 3 件
220x4+223x2+44+35+221 = 9 件
100x30x63=115 = 1 件
100x30x63=504x5+588+587+502+420+382+378+280+254+237+174 = 15 件


每次啟動要自動：

建表
補欄位
補索引
不刪資料
不清表
不重建後丟資料
SQLite / PostgreSQL 都支援
2. inventory 欄位

至少確認：

id
customer_name
customer_uid
product_text
product_code
material
month_tag
qty
area
location
source
note
operator
created_at
updated_at
3. orders 欄位

至少確認：

id
customer_name
customer_uid
product_text
product_code
material
month_tag
qty
area
location
source
note
operator
created_at
updated_at
4. master_orders 欄位

至少確認：

id
customer_name
customer_uid
product_text
product_code
material
month_tag
qty
area
location
source
note
operator
created_at
updated_at
5. warehouse_cells 欄位

至少確認：

id
zone
band
column_index
row_name
slot
slot_no
slot_type
customer_name
product_text
material
qty
placement_label
items_json
note
created_at
updated_at
6. shipping_records 欄位

至少確認：

id
customer_name
product_text
material
qty
source_table
before_qty
after_qty
note
operator
created_at
7. today_changes 欄位

至少確認：

id
action
table_name
customer_name
product_text
detail_json
operator
created_at
unread
8. 必補索引
inventory material
inventory area / location
inventory customer_name
orders customer_name / updated_at
orders material
orders area / location
master_orders customer_name / updated_at
master_orders material
master_orders area / location
warehouse_cells zone / band / slot
warehouse_cells zone / column_index / slot
warehouse_cells slot_type
shipping_records customer_name / created_at
today_changes created_at / unread
十、後端 API 需要改什麼
1. 庫存 API

需要支援：

新增庫存
編輯庫存
刪除庫存
批量編輯
批量刪除
批量套用材質
移到A區
移到B區
加到訂單
加到總單
2. 訂單 API

需要支援：

新增訂單
編輯訂單
刪除訂單
批量編輯
批量刪除
批量套用材質
移到A區
移到B區
加到總單
直接出貨
3. 總單 API

需要支援：

新增總單
編輯總單
刪除總單
批量編輯
批量刪除
批量套用材質
移到A區
移到B區
直接出貨
4. 倉庫 API

需要支援：

讀取格子
儲存格位
新增格子
插入格子
刪除格子
移動商品
移除格內商品
讀取未入倉商品
安全補缺格
查 A/B 統計
5. 出貨 API

需要支援：

出貨預覽
出貨確認
寫 shipping_records
扣 inventory / orders / master_orders
寫 today_changes
十一、前端 JS 需要改什麼
1. 只能保留一套主 renderer

每頁只保留一套主要 renderer：

庫存 renderer
訂單 renderer
總單 renderer
出貨 renderer
倉庫 renderer
今日異動 renderer

不要多套互相覆蓋。

2. 禁止補丁式寫法

不要：

setInterval 塞按鈕
MutationObserver 塞按鈕
hardlock overlay
多個檔案同時改同一 DOM
同一按鈕綁多次 click
3. 事件要寫在主檔

所有按鈕事件要直接寫在主檔：

庫存 JS
訂單 JS
總單 JS
出貨 JS
倉庫 JS
4. Undo

如果保留 undo，必須定義：

pushProductUndo
popProductUndo / restore

如果不用 undo，就要移除所有呼叫。
不能再出現：

pushProductUndo is not defined







十四、測試清單

改完一定要測：

庫存
新增商品成功
清單立刻出現
重新整理還在
批量編輯後立刻退出
批量刪除成功
套用材質成功
移到 A/B 區成功
加到訂單成功
加到總單成功
訂單
新增訂單成功
點客戶立刻顯示商品
加到總單成功
移到 A/B 區成功
批量編輯成功
批量刪除成功
直接出貨成功
總單
新增總單成功
點客戶立刻顯示商品
移到 A/B 區成功
批量編輯成功
批量刪除成功
直接出貨成功
倉庫
A/B 各 6 欄
每欄預設 20 格
點格子開編輯
批量加入商品成功
儲存後刷新還在
插入格子成功
刪除空格成功
有商品格不能刪
拖拉成功
未入倉下拉正確移除 / 回復
出貨
出貨頁不卡
選客戶有商品
預覽正確
確認後 DB 扣數
shipping_records 有紀錄
今日異動有紀錄
十四、測試清單

改完一定要測：

庫存
新增商品成功
清單立刻出現
重新整理還在
批量編輯後立刻退出
批量刪除成功
套用材質成功
移到 A/B 區成功
加到訂單成功
加到總單成功
訂單
新增訂單成功
點客戶立刻顯示商品
加到總單成功
移到 A/B 區成功
批量編輯成功
批量刪除成功
直接出貨成功
總單
新增總單成功
點客戶立刻顯示商品
移到 A/B 區成功
批量編輯成功
批量刪除成功
直接出貨成功
倉庫
A/B 各 6 欄
每欄預設 20 格
點格子開編輯
批量加入商品成功
儲存後刷新還在
插入格子成功
刪除空格成功
有商品格不能刪
拖拉成功
未入倉下拉正確移除 / 回復
出貨
出貨頁不卡
選客戶有商品
預覽正確
確認後 DB 扣數
shipping_records 有紀錄
今日異動有紀錄





