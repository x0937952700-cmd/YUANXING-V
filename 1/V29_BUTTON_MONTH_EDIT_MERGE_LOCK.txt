V29 button/month/edit/merge lock

本版只針對使用者指定問題修正，不新增舊 renderer，不恢復舊 FIX 載入。

已改：
1. templates/base.html
   - 所有 CSS/JS/PWA 版本改為 full-master-v29-button-month-edit-merge-lock。
   - style.css 後面新增 yx_final_ui_lock.css，確保最後載入、不被舊版按鈕樣式覆蓋。

2. static/yx_final_ui_lock.css
   - 全頁背景固定鋪滿整個介面。
   - 所有按鈕保留金色外框，內圈改成夢幻膠囊圖。
   - 重要按鈕紅字，一般按鈕黑字。
   - 工具列靠左。
   - 月份標籤 yx-month-tag 樣式。

3. static/yx_pages/page_inventory_master_v22.js / page_orders_master_v22.js / page_master_order_master_v22.js
   - 商品尺寸含 1月～12月時，顯示成材質右邊的月份標籤，尺寸欄只顯示尺寸。
   - 批量編輯儲存後不退出編輯狀態、不重畫整張表，避免正在選取或輸入的欄位被刷新打斷。
   - 後端回傳快照時，若正在編輯，只更新資料與表頭件數，不重建 DOM。
   - 新增同尺寸＋材質偵測；提交前會詢問是否合併。確定＝合併，取消＝分開新增保存。

4. app.py / db.py
   - /api/inventory 支援 duplicate_mode，取消合併時庫存也能分開新增，不會被後端自動合併。
   - PostgreSQL / SQLite migration 自動補 month_tag 欄位。
   - 編輯、批量編輯、庫存/訂單/總單保存時同步處理 month_tag。

5. service-worker / pwa / manifest / render / wsgi / requirements
   - 版本與啟動設定保留穩定，只更新快取版本與說明。

驗證：
- Python compile 通過
- JS syntax check 通過
- smoke test 通過
