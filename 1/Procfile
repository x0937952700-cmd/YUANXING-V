web: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 120

# V125 mobile zoom: start command unchanged.

# V152 network payload compression pack: keep deployment config on mainline.
