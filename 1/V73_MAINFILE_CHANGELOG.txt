V73 訂單/總單按鈕根本修復版

問題：訂單/總單按鈕一直回不來，是因為舊 renderer 只重畫 summary table，沒有產生 .yx128-summary-controls，V70 fallback 只會改已存在的 controls，所以畫面上沒有容器可放按鈕。

修正：
1. 訂單/總單 JS 追加 V73 hardlock，若 .yx128-summary-controls 不存在，會自動建立。
2. 訂單圖一區固定：移到A區、移到B區、加到總單、批量刪除、批量編輯全部。
3. 總單圖一區固定：移到A區、移到B區、批量刪除、批量編輯全部。
4. template module.html 的總單 toolbar 也補上相同按鈕，避免初始 HTML 缺失。
5. CSS 強制顯示與換行，避免舊 CSS 隱藏。
6. 快取版本更新 V73。

未刪除資料庫與 API；只補強 HTML/JS/CSS 顯示與事件入口。
