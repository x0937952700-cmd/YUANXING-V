HTML_DIRECT_LOCK 修改說明

1. 已把庫存 / 訂單 / 總單的新商品表格介面與按鈕直接寫入 templates/module.html。
   之後要改商品表格、批量編輯、批量刪除、套用材質、A/B 篩選等介面，直接改 templates/module.html。

2. 已修改 static/yx_commercial_ui_lock.js：
   - 不再用 JS 建立 yx-main-table 外殼。
   - 選客戶時只更新 HTML 既有表格標題與 tbody 資料。
   - 保留資料載入、API、送出、出貨預覽等功能。

3. 已在 templates/base.html 停用會重建整頁/表格/倉庫外殼的舊版二次渲染 JS：
   yx_pack13, 14, 15, 16, 17, 19, 20, 21, 22, 25, 26, 27。
   CSS 先保留，避免樣式遺失；真正功能以 clean_ui_static.js + yx_commercial_ui_lock.js + pack28/29/30 為主。

4. 保留必要資料渲染：tbody 商品列、客戶列表、出貨預覽、倉庫格內資料仍需 JS 根據資料庫填入，這不屬於舊版介面覆蓋；介面外殼與按鈕已改成 HTML 直出。

5. 已通過 Python compileall：app.py / db.py / wsgi.py 無語法錯誤。
