FIX76 合併提示 / 倉庫儲存 / 出貨已選清理修正版

1. 庫存 / 訂單 / 總單
- 偵測到相同來源、相同客戶/位置、相同尺寸+材質+商品資料時，會詢問是否合併。
- 按確認後會合併數量，保留一筆，刪除重複筆。

2. 倉庫圖
- 修正「儲存格位」送出時 column_index 沒帶到後端，造成格位參數錯誤。
- 儲存後會重新渲染倉庫圖與未錄入清單。

3. 出貨
- 已選商品區移除缺貨卡片、上移、下移、移除按鈕。
- 保留一鍵清空；商品內容請直接在上方商品資料欄編輯。

4. 快取
- base.html / pwa.js / service-worker.js / manifest 已更新到 FIX76。
